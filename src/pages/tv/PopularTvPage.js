import React from 'react';

//TODO: wrapped in a HOC because layout of all the info pages for movies, tvs and people are
// similar
const PopularTvPage = props => (
  <div>
    <h1>Popular tv shows are showed here</h1>
  </div>
);

export default PopularTvPage;