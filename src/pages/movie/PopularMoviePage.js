import React from 'react';

//TODO: wrapped in a HOC because layout of all the info pages for movies, Movies and people are
// similar
const PopularMoviePage = props => (
  <div>
    <h1>Popular movies are showed here</h1>
  </div>
);

export default PopularMoviePage;